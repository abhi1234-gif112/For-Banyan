import type { ReactNode } from 'react';import { useQuery } from '@tanstack/react-query';import { Activity, Bell, FileText, Users } from 'lucide-react';import { api } from './lib/api';
type Dashboard={mentions:number;alerts:number;individuals:number};
function Stat({label,value,icon}:{label:string;value:number;icon:ReactNode}){return <div className="card"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p>{icon}</div><p className="mt-4 text-4xl font-bold">{value}</p></div>}
export default function App(){const {data,isLoading}=useQuery({queryKey:['dashboard'],queryFn:async()=>{const r=await api.get<{data:Dashboard}>('/dashboard');return r.data.data;},retry:false});return <main className="min-h-screen p-6"><section className="mx-auto max-w-7xl"><div className="mb-8"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-cyan-400">Saptanga Labs LLP</p><h1 className="mt-3 text-5xl font-black">NAZAR Political Intelligence</h1><p className="mt-4 max-w-3xl text-slate-300">Enterprise digital monitoring for Indian public figures, alerts, mentions, reports and analytics.</p></div><div className="grid gap-4 md:grid-cols-3"><Stat label="Mentions" value={data?.mentions ?? 0} icon={<Activity/>}/><Stat label="Open Alerts" value={data?.alerts ?? 0} icon={<Bell/>}/><Stat label="Individuals" value={data?.individuals ?? 0} icon={<Users/>}/></div><div className="card mt-6"><div className="flex items-center gap-3"><FileText/><h2 className="text-2xl font-bold">Operations Console</h2></div><p className="mt-3 text-slate-300">{isLoading?'Loading live intelligence data...':'Use the authenticated REST API to manage keywords, mentions, reports, analytics and alerts.'}</p></div></section></main>}
